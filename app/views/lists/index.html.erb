<p style="color: green"><%= notice %></p>

<h1>To Do Lio Sha</h1>

<table class="table-bordered">
  <thead>
    <tr>
      <th>Todo Item</th>
      <th>Completed</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lists.each do |list| %>
    <!-- <%= render list %> -->
      <tr>
        <td><%= list.description %></td>
        <td><%= list.completed %></td>
        <td><%= link_to "Show", edit_list_path(list) %></td> 
        <td><%= link_to "Edit", edit_list_path(list) %></td> 
        <td><%= link_to "Destroy", list, method: :delete %></td>
      </tr>
    <% end %>
  </tbody>

</table>
<%= link_to "Add New", new_list_path %>
