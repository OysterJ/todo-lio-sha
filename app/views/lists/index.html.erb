<p style="color: green"><%= notice %></p>

<h1>To Do Lio Sha</h1>

<table class="table table-bordered">


  <tbody>
    <% @lists.each do |list| %>
    <!-- <%= render list %> -->
      <% if list.completed %>
      <tr class="table-secondary" >
        <td class="striker"><%= list.description %></td>
        <td>
          <%= link_to "Show", edit_list_path(list) %> 
          <%= link_to "Edit", edit_list_path(list) %> 
          <%= link_to "Destroy", list, method: :delete %></td>
      </tr>
      <% else %>
       <tr>
        <td><%= list.description %></td>
        <td>
          <%= link_to "Show", edit_list_path(list) %>
          <%= link_to "Edit", edit_list_path(list) %> 
          <%= link_to "Destroy", list, method: :delete %></td>
      </tr>
      <% end %>
    <% end %>
  </tbody>

</table>
<%= link_to "Add New", new_list_path, class: "btn btn-secondary" %>
